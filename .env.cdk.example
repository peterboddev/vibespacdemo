# CDK Environment Configuration
# Copy this file to .env.cdk and update with your values

# Target environment (dev, test, prod)
ENVIRONMENT=dev

# AWS Configuration (optional - will use AWS CLI defaults if not specified)
# CDK_DEFAULT_ACCOUNT=123456789012
# CDK_DEFAULT_REGION=us-east-1
# AWS_DEFAULT_REGION=us-east-1

# Deployment Configuration
# CDK_DEPLOY_REQUIRE_APPROVAL=never
# CDK_DEPLOY_ROLLBACK=true